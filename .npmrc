# Shared pnpm store across the workspace
store-dir=~/.pnpm-store

# All workspace packages share a single lockfile at the root
shared-workspace-lockfile=true

# Use workspace: protocol for cross-package references
link-workspace-packages=true
save-workspace-protocol=rolling

# Reproducible installs — fail if lockfile is out of date
frozen-lockfile=false
